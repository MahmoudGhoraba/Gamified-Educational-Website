@model IEnumerable<Spaghetti.Models.Ranking>

@{
    ViewData["Title"] = "rank";
}

<h1>filter</h1>

<table class="table">
    <thead>
    <tr>
        <th>CourseID</th>
        <th>Placement</th>
        <th>Your Points</th>
    </tr>
    </thead>
    <tbody>
    @foreach (var rank in Model)
    {
        <tr>
            <td>@rank.CourseId</td>
            <td>@rank.Rank</td>
            <td>@rank.TotalPoints</td>
        </tr>
    }
    </tbody>
</table>
